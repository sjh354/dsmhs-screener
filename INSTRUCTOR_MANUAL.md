# DSMHS Screener — 강사용 운영 메뉴얼 (v1.2)

## 1. 시스템 구성

```
[학생 서버 A] ──┐
[학생 서버 B] ──┼──(register / heartbeat)──▶ [강사 서버 :4000]
[학생 서버 C] ──┘                                     │
                                         브라우저 대시보드 (표 형태)
```

학생은 `package.json`에 `"dsmhs"` 설정 추가 후 `require('dsmhs-screener')`를 앱 최상단에 추가하기만 하면 된다.  
강사 서버는 학생들의 등록과 heartbeat를 수신하고, 각 학생 서버의 API 엔드포인트를 실시간으로 프로브해 결과를 표로 표시한다.

---

## 2. 강사 서버 설치 및 실행

```bash
cd instructor-server
npm install
npm start
```

서버가 시작되면 브라우저에서 `http://localhost:4000` 접속.

포트를 변경하려면:

```bash
PORT=5000 npm start
```

---

## 3. 대시보드 사용법

### 화면 구성

```
┌────────────────────────────────────────────────────────────────┐
│ DSMHS 엔드포인트 테스트  |  5초마다 자동 갱신  |  [+ 추가 폼]  │
├──────────┬────────────┬───────────┬────────────┬───────────────┤
│  학생    │ GET /health│ GET /memos│ POST /memos│      …        │
├──────────┼────────────┼───────────┼────────────┼───────────────┤
│ 홍길동 🟢│     O      │     O     │     X      │      O        │
│ 이지은 🔴│     —      │     —     │     —      │      —        │
│ 박민수 🟢│     O      │     X     │     O      │      O        │
└──────────┴────────────┴───────────┴────────────┴───────────────┘
```

- **행**: 각 학생 (이름 가나다순)
- **열**: 테스트할 API 엔드포인트
- **좌측 학생 열**: 가로 스크롤해도 고정
- **상단 헤더**: 세로 스크롤해도 고정

### 셀 의미

| 표시 | 색상 | 의미 |
|------|------|------|
| **O** | 초록 | HTTP 2xx 응답 — 엔드포인트 정상 |
| **X** | 빨강 | 비-2xx 또는 연결 실패 |
| **—** | 회색 | 학생 서버 오프라인 (프로브 생략) |
| **…** | 회색 점멸 | 프로브 진행 중 |

### 자동 갱신

5초마다 학생 목록을 갱신하고, 온라인 학생에 대해 모든 엔드포인트를 자동으로 프로브한다.

---

## 4. 엔드포인트 관리

### 기본 엔드포인트

대시보드 첫 로드 시 아래 5개가 자동으로 설정된다 (server/index.js 기준):

| 메서드 | 경로 | 요청 body |
|--------|------|-----------|
| GET | /health | — |
| GET | /memos | — |
| GET | /memos/1 | — |
| POST | /memos | `{"title":"test","content":"test"}` |
| DELETE | /memos/1 | — |

### 엔드포인트 추가

헤더 오른쪽 폼에서:
1. 메서드 선택 (GET / POST / PUT / PATCH / DELETE)
2. 경로 입력 (예: `/hello`, `/memos/2`)
3. body가 필요한 경우 JSON 형식으로 입력 (선택)
4. **+ 추가** 버튼 클릭

추가된 목록은 브라우저 localStorage에 저장되어 새로고침 후에도 유지된다.

### 엔드포인트 삭제

열 헤더의 **×** 버튼을 클릭하면 해당 열이 즉시 삭제된다.

---

## 5. 수업 시작 전 체크리스트

1. 강사 서버 실행 확인 (`npm start`, 포트 4000)
2. 학생들에게 강사 PC의 실제 IP 공유 (`instructorUrl` 설정용)
3. 학생 서버 실행 후 대시보드에서 이름·접속 상태 확인
4. 이번 수업에서 사용할 엔드포인트 목록 정리 (필요 시 기본값 외 추가)
5. 오프라인(—) 학생이 있으면 설정 확인 요청

---

## 6. 학생 설정 안내

학생 프로젝트의 `package.json`에 `"dsmhs"` 키를 추가:

```json
{
  "name": "my-app",
  "dsmhs": {
    "studentName": "홍길동",
    "instructorUrl": "http://192.168.1.100:4000"
  }
}
```

- `instructorUrl`에 강사 PC의 실제 IP 사용 (`localhost` 사용 금지)
- 학생 서버 포트는 `PORT` 환경변수에서 자동으로 읽음

---

## 7. API 직접 조회

```bash
# 학생 목록
curl http://localhost:4000/screener/students

# 특정 학생의 엔드포인트 수동 프로브
curl -X POST http://localhost:4000/screener/probe \
  -H 'Content-Type: application/json' \
  -d '{"studentName":"홍길동","path":"/health","method":"GET"}'
```

probe 응답:
```json
{ "ok": true, "status": 200 }
{ "ok": false, "status": 404 }
{ "ok": false, "status": 0, "error": "timeout" }
```

---

## 8. FAQ

**Q. 학생이 등록되지 않아요.**
- 학생 `package.json`의 `instructorUrl`이 강사 PC의 실제 IP로 설정되어 있는지 확인 (`localhost` 사용 금지).
- 방화벽에서 포트 4000이 열려 있는지 확인.

**Q. 학생은 접속 중(🟢)인데 셀이 전부 X예요.**
- 학생 서버의 포트가 올바르게 등록되었는지 확인. 학생 서버를 재시작하면 포트가 재등록된다.
- 해당 엔드포인트가 학생 서버에 실제로 구현되어 있는지 확인.

**Q. 엔드포인트 목록이 초기화되었어요.**
- localStorage를 지우면 기본 5개로 리셋된다. 수업 시작 전 목록을 저장해 두는 것을 권장한다.

**Q. 강사 서버가 꺼진 후 다시 켜면 학생들이 재등록되나요?**
- heartbeat 수신 시 등록 정보가 없으면 자동으로 재등록된다 (포트 정보 없이).
- 정확한 포트 정보가 필요하면 학생이 서버를 재시작해야 한다.

**Q. IP가 127.0.0.1로 등록되어요.**
- 학생 PC가 네트워크에 연결되지 않은 상태다. Wi-Fi나 유선 네트워크 연결 후 서버를 재시작하도록 안내한다.
