# DSMHS Screener — 강사용 운영 메뉴얼 (v1.3)

## 1. 시스템 구성

```
[학생 서버 A] ──┐
[학생 서버 B] ──┼──(register / heartbeat)──▶ [강사 서버 :4000]
[학생 서버 C] ──┘                                     │
                                         브라우저 대시보드 (표 형태)
```

학생은 `package.json`에 `"dsmhs"` 설정 추가 후 `import 'dsmhs-screener'`를 앱 최상단에 추가하기만 하면 된다.  
강사 서버는 학생들의 등록과 heartbeat를 수신하고, 각 학생 서버의 API 엔드포인트를 실시간으로 프로브해 결과를 표로 표시한다.

---

## 2. 강사 서버 설치 및 실행

```bash
cd instructor-server
npm install
npm start
```

서버가 시작되면 브라우저에서 `http://localhost:4000` 접속.

포트를 변경하려면:

```bash
PORT=5000 npm start
```

---

## 3. 대시보드 사용법

### 암호 화면

대시보드 첫 접속 시 암호 입력 화면이 표시된다.

- **암호**: `2026spring`
- 세션 중 한 번만 입력하면 되며 탭을 닫으면 초기화된다.

### 화면 구성

```
┌──────────────────────────────────────────────────────────────────────────┐
│ DSMHS 엔드포인트 테스트  │ 5초마다 자동 갱신 │ [설정 업로드] [설정 다운로드] │
├──────────┬────────────┬───────────┬────────────┬────────────────────────┤
│  학생    │ GET /health│ GET /memos│ POST /memos│          …             │
├──────────┼────────────┼───────────┼────────────┼────────────────────────┤
│ 홍길동 🟢│     O      │     O     │     X      │          O             │
│ 이지은 🔴│     —      │     —     │     —      │          —             │
│ 박민수 ⬜│    (미접속) │           │            │                        │
└──────────┴────────────┴───────────┴────────────┴────────────────────────┘
```

- **🟢** 온라인 (heartbeat 정상)
- **🔴** 오프라인 (접속했다 끊김)
- **⬜** 미접속 (명단에 있지만 아직 한 번도 연결 안 됨)

### 셀 의미

| 표시 | 색상 | 의미 |
|------|------|------|
| **O** | 초록 | HTTP 2xx 응답 — 엔드포인트 정상 |
| **X** | 빨강 | 비-2xx 또는 연결 실패 |
| **—** | 회색 | 학생 서버 오프라인 (프로브 생략) |
| **…** | 회색 점멸 | 프로브 진행 중 |

### 자동 갱신

5초마다 학생 목록을 갱신하고, 온라인 학생에 대해 모든 엔드포인트를 자동으로 프로브한다.

---

## 4. 수업 설정 관리 (JSON 파일)

수업 시작 전 학생 명단과 엔드포인트를 JSON 파일로 한 번에 업로드한다.  
`example-config.json`을 템플릿으로 수정해서 사용한다.

### 설정 파일 형식

```json
{
  "students": ["홍길동", "이지은", "박민수"],
  "endpoints": [
    { "method": "GET", "path": "/health" },
    { "method": "GET", "path": "/memos" },
    { "method": "POST", "path": "/memos", "body": {"title":"test","content":"test"} },
    { "method": "DELETE", "path": "/memos/1" }
  ]
}
```

### 설정 업로드

1. 헤더의 **설정 업로드** 버튼 클릭
2. 준비한 JSON 파일 선택
3. 성공 메시지 확인 → 명단과 엔드포인트가 즉시 반영됨

### 설정 다운로드

헤더의 **설정 다운로드** 버튼을 클릭하면 현재 서버에 적용된 설정을 JSON 파일로 저장한다.  
수업마다 설정 파일을 보관해두면 다음 수업 시 빠르게 복원할 수 있다.

> **주의**: 설정은 서버 메모리에만 저장된다. 서버를 재시작하면 초기화되므로 수업 시작 시 매번 업로드해야 한다.

---

## 5. 수업 시작 전 체크리스트

1. 강사 서버 실행 확인 (`npm start`, 포트 4000)
2. 브라우저에서 대시보드 접속 → 암호(`2026spring`) 입력
3. **설정 업로드** 버튼으로 수업 설정 JSON 파일 업로드
4. 학생들에게 강사 서버 주소 공유 (`instructorUrl` 설정용)
5. 학생 서버 실행 후 대시보드에서 이름·접속 상태 확인
6. 미접속(⬜) 학생이 있으면 설정 확인 요청

---

## 6. 학생 설정 안내

학생 프로젝트의 `package.json`에 `"dsmhs"` 키를 추가:

```json
{
  "name": "my-app",
  "dsmhs": {
    "studentName": "홍길동",
    "instructorUrl": "http://192.168.1.100:4000"
  }
}
```

서버 진입점 최상단에 한 줄 추가:

```js
// CJS
require('dsmhs-screener');

// ESM
import 'dsmhs-screener';
```

- `instructorUrl`에 강사 PC의 실제 IP 사용 (`localhost` 사용 금지)
- 학생 서버 포트는 `PORT` 환경변수에서 자동으로 읽음

---

## 7. API 직접 조회

```bash
# 학생 목록 (명단 미접속 학생 포함)
curl http://localhost:4000/screener/students

# 현재 수업 설정 조회
curl http://localhost:4000/screener/config

# 수업 설정 업로드
curl -X POST http://localhost:4000/screener/config \
  -H 'Content-Type: application/json' \
  -d @my-class-config.json

# 특정 학생의 엔드포인트 수동 프로브
curl -X POST http://localhost:4000/screener/probe \
  -H 'Content-Type: application/json' \
  -d '{"studentName":"홍길동","path":"/health","method":"GET"}'
```

probe 응답:
```json
{ "ok": true, "status": 200 }
{ "ok": false, "status": 404 }
{ "ok": false, "status": 0, "error": "timeout" }
```

---

## 8. FAQ

**Q. 학생이 등록되지 않아요.**
- 학생 `package.json`의 `instructorUrl`이 강사 PC의 실제 IP로 설정되어 있는지 확인 (`localhost` 사용 금지).
- 방화벽에서 포트 4000이 열려 있는지 확인.

**Q. 학생은 접속 중(🟢)인데 셀이 전부 X예요.**
- 학생 서버의 포트가 올바르게 등록되었는지 확인. 학생 서버를 재시작하면 포트가 재등록된다.
- 해당 엔드포인트가 학생 서버에 실제로 구현되어 있는지 확인.

**Q. 서버 재시작 후 명단과 엔드포인트가 사라졌어요.**
- 설정은 서버 메모리에만 저장된다. 서버 재시작 시 JSON 파일로 재업로드해야 한다.
- 수업 전 저장해 둔 설정 파일을 **설정 업로드** 버튼으로 다시 올리면 된다.

**Q. 강사 서버가 꺼진 후 다시 켜면 학생들이 재등록되나요?**
- heartbeat 수신 시 등록 정보가 없으면 자동으로 재등록된다 (포트 정보 없이).
- 정확한 포트 정보가 필요하면 학생이 서버를 재시작해야 한다.

**Q. IP가 127.0.0.1로 등록되어요.**
- 학생 PC가 네트워크에 연결되지 않은 상태다. Wi-Fi나 유선 네트워크 연결 후 서버를 재시작하도록 안내한다.
